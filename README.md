# vimrc

#### Dependencies from the AUR repository:
+ vim-airline
+ vim-airline-themes
+ vim-jellybeans
+ vim-latexsuite

#### Install guide:
```
git clone https://github.com/Hogfeldt/vimrc/
ln -s ~/vimrc/.vimrc .vimrc
```
